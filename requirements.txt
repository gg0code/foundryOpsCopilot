# Minimum versions — pip picks the newest with prebuilt wheels for your Python.
# (Pinning exact versions broke on Python 3.13/3.14 because old pandas/numpy
# wheels weren't published for those interpreters.)
flask>=3.0
flask-cors>=4.0
pandas>=2.1
numpy>=1.26
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.1
shap>=0.44
joblib>=1.3
# Powers the /ask conversational endpoint (AI That Converses).
# Optional at runtime — the rest of the demo runs without either. /api/ask uses
# Claude if ANTHROPIC_API_KEY is set, else Groq (OpenAI-compatible) if GROQ_API_KEY
# is set; otherwise it returns a friendly "not configured" message.
anthropic>=0.40   # Claude path
openai>=1.30      # Groq path (points the OpenAI SDK at https://api.groq.com/openai/v1)
python-dotenv>=1.0  # loads keys from a git-ignored .env in the project root
